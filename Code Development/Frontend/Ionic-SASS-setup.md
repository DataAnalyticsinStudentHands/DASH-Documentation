## Setup Ionic project to use SASS for customization

Prerequisites:

`npm install`    
`bower install`

Change into the project directory and get started by typing:
 
`ionic setup sass`

This will load all necessary modules via npm.